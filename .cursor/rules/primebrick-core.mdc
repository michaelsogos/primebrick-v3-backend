---
description: Primebrick backend — product context (always apply)
alwaysApply: true
---

# Primebrick backend

- **Purpose:** Modular management system (CRM, Accounting, Warehouse, custom modules).
- **Stack:** Express + TypeScript. UI lives in a separate frontend repo.
- **Before cloud/Foundry/messaging work:** read `docs/ai/SKILLS.md` — only **[x]** skills are in scope unless the user names another path.
- **API / scripts:** follow **`.cursor/rules/backend-api.mdc`** when editing `src/**` or `scripts/**`.
- **Onboarding:** skim **`AGENTS.md`**; GitFlow is **`.cursor/rules/gitflow-guard.mdc`** only.
- **Team-facing `*.md`:** English only; non-English reserved for direct chat with the user.

## Schema diff safety

If `db-meta/diff-entities-vs-database.json` includes `renameHeuristicUserReviewRequired: true`, stop and **ask the user** before applying heuristic renames / `ALTER … RENAME COLUMN`.
